## Quickstart

```bash
# One-command usage (no install)
python vm_to_slot.py humboldt-vm-parallel-seg.xml > out.json

# Or use the package CLI
python -m slotbuilder.cli humboldt-vm-parallel-seg.xml --out out.json --indent 2